/noRule.kt:(238,269): error: Cannot transform Order → OrderDto:
    OrderDto.count: Long — no rule transforms Int into Long.
    OrderDto.tags: List<String> — no rule transforms List<Int> into List<String>.

/noRule.kt:(298,323): error: Cannot transform Int → String:
    String — no rule transforms Int into String.

/noRule.kt:(402,432): error: Cannot transform Box<Int> → Box<Long>:
    Box<Long>.value: Long — no rule transforms Int into Long.

/noRule.kt:(544,573): error: Cannot transform Maybe → Surely:
    Surely.name: String — no rule transforms String? into String.
